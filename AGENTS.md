## Boundaries

Read [`docs/BOUNDARIES.md`](docs/BOUNDARIES.md) before adding features that touch both SvelteKit and the API package. It defines which layer owns business logic, authorization, validation, and data access.

## Task Management

Use `/dex` to break down complex work, track progress across sessions, and coordinate multi-step implementations. Commit `dex` metatdata along with accompanying code.
