---
id: a-9445
status: closed
deps: [a-8ef8]
links: []
created: 2026-01-08T06:09:03Z
type: task
priority: 2
assignee: Jesse Hoyos
parent: a-f9c0
---
# Add unit tests for submitTrack schema

Add tests for the updated submitTrack schema validation.

## Design

File: src/lib/remote/stage.remote.test.ts

Tests:
- accepts valid submission with URL only
- accepts valid submission with URL and note
- rejects note over 280 characters
- rejects invalid Spotify URL
- requires stageId

Pattern from existing battle.remote.test.ts:
  import { describe, expect, it } from 'vitest';
  import * as v from 'valibot';
  
  describe('submitSchema', () => {
    it('accepts valid data', () => {
      const result = v.safeParse(submitSchema, {...});
      expect(result.success).toBe(true);
    });
  });

## Acceptance Criteria

- Tests pass with bun test:unit
- Schema validation behavior verified

