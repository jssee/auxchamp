# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  jobs:
    - name: biome
      run: bun biome check --diagnostic-level=error --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
