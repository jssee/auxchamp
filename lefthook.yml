# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  jobs:
    - name: oxlint
      run: bun oxlint --fix {staged_files}
      stage_fixed: true
    - name: oxfmt
      glob: "*.{js,ts,jsx,tsx,mjs,mts,cjs,cts,json}"
      run: bun oxfmt --write {staged_files}
      stage_fixed: true
